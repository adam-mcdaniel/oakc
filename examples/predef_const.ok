

#[if(DATE_MONTH == 8) {
    #[error("this code doesn't compile in august")]
}]


fn main() {
    putstr("compiled on: ");
    putnum(DATE_MONTH); putchar('/');
    putnum(DATE_DAY);   putchar('/');
    putnum(DATE_YEAR);
}