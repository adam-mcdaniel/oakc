#[heap(32)]

fn ln() { putchar('\n') }

fn fact(n: num) -> num {
    if le(n, 1) { 1 }
    else { n * fact(n-1) }
}

fn main() {
    putstr("result: "); putnum(fact(5)); ln();
    for (let i: num = 0; i-10; i=i+1) {
        putnum(i); putstr("! is "); putnum(fact(i)); ln();
    }
}