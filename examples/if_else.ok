#[std]

fn not(x: bool) -> num {
    let result = 0;
    if x {
        putstrln("x is true");
        result = 1
    } else {
        putstrln("x is false");
    }
    return result
}

fn main() {
    putnumln(not(true));
    putnumln(not(false));
}